# יומן החלטות — CFO App

מסמך זה מפרט **מה הוחלט ולמה**, כולל חלופות שנשקלו ונדחו. הוא לא קובע מה הושלם או מה עובד — סטטוס נקבע רק בבדיקה מחדש.

---

## סשן קודם (תמצית)

- **דשבורד — קבצים לפי חודש**: טבלה לפי חודש נבחר + התרעה על מקור חוזר (2+ חודשים בעבר) שחסר החודש. סיבה: לוודא שכל המקורות הועלו וכמה הוצא מכל אחד.
- **זיהוי פורמט per-tab**: כל טאב מזהה פריסט בעצמו, לא "מזהים מהטאב הראשון וכופים על כולם". סיבה: לטאבים שונים באותו קובץ יכולה להיות כותרת שונה לאותו שדה ("תאריך עסקה" מול "תאריך רכישה"). נגזרת: כל תנועה נושאת את ה-`source` שהתאים לה ספציפית.
- **העלאה חוזרת בשם זהה** מחליפה (PATCH) את הקובץ ב-Drive imports, לא יוצרת עותק.
- **`autoPort: true`** ב-`.claude/launch.json` (מקומי, לא ב-git) — פורט 5500 היה תפוס.

---

## ייבוא וניתוח קבצים

- **הסיבה ל"טאב 2 מניב 0 שורות" היא השערה (ב) מהיומן הקודם** — הטאב תואם פריסט אך שורותיו לא נקראו. שורש: `parseWorksheet` קרא רק `<v>` והתעלם מתאים `t="inlineStr"`, שבהם הטקסט יושב ב-`<is><t>`. תא טקסט חזר ריק → שורת הכותרות (שכולה טקסט) נמחקה כשורה ריקה → "לא זוהה פורמט".
  - נדחו: השערה (א) cache בדפדפן, והשערה (ג) שהטאבים מכילים אותן תנועות.
- **`Cache-Control: no-store` בשרת הפיתוח**. סיבה: היעדר מוחלט של cache headers הפך "JS ישן" לחשוד קבוע וגזל זמן איתור. בפיתוח הקובץ בדיסק הוא האמת היחידה.
- **חיפוש ה-EOCD ב-ZIP חסום ל-64KB האחרונים** (מקסימום לפי תקן). סיבה: קובץ שאינו ZIP נסרק בייט-בייט עד תחילתו לפני שנדחה.
- **מפתח מפה = `JSON.stringify([month, file])`**, לא שרשור עם תו מפריד. סיבה: שם קובץ יכול להכיל כל תו, אז אין מפריד ליטרלי בטוח. (המפריד הקודם היה בייט NUL, שהפך את הקובץ ל"בינארי" בעיני git.)
- **חסימת כפילויות גם בתוך אותה קבוצת ייבוא**, לא רק מול בסיס הנתונים. סיבה: אחרי שטאב 2 התחיל להיקרא, שני טאבים חופפים באותו קובץ ייצרו שתי שורות עם אותו `tx_id`.
- **זהות תנועה = גיבוב + מספר ההופעה בתוך הטאב שלה** (`hash`, `hash#2`, ...). סיבה: חסימה לפי גיבוב בלבד מחקה שני חיובים זהים אמיתיים (אותו בית עסק, אותו יום, אותו סכום). חזרה **בתוך** טאב = שתי תנועות; חזרה **בין** טאבים = אותה תנועה ומתקבצת, כי סדרת ההופעות של הטאב השני מייצרת אותם מזהים. ההופעה הראשונה שומרת את הגיבוב החשוף, כדי שמזהים שכבר נשמרו בייבוא קודם ימשיכו להתאים.
  - נדחה: הוספת שדה מבחין (מספר שורה/שובר) לגיבוב — היה הופך כל ייבוא חוזר של אותו קובץ לתנועות חדשות.
- **גיבוב ה-SHA-256 מקבילי ולא סדרתי**. סיבה: `await` לכל מועמד בלולאה סידר את כל הקבוצה בטור, והגיבובים לא תלויים זה בזה.
- **שורה שלא נקראה נספרת ומדווחת, לא נזרקת.** `normalizeRows` מחזיר `{candidates, skipped, dataRowCount}` כך ששני הראשונים מתחברים לשלישי. סיבה: `continue` שקט על שורה עם תאריך/סכום/בית עסק לא קריא הוציא כסף שיצא מהחשבון מכל סיכום באפליקציה בלי שום עקבה.
- **`import_log` במצב** — רשומה לכל קובץ: כמה שורות נתונים היו בו ומה עלה בגורלן. סיבה: בלעדיו הדיווח על שורות שלא נקראו נעלם ברגע שמסך הייבוא נסגר.
- **טאב שלא זוהה בו פורמט נספר כשורות שלא נקראו** (`unreadable_sheets`), ולא כ-"0 שורות". סיבה: "0" נקרא כמו "הטאב היה ריק".
- **סיכום הייבוא מוצג כמשוואה שנסגרת**: שורות בקובץ = סווגו אוטומטית + ממתינות לסיווג + כפילויות שנחסמו + לא נקראו. סיבה: מספר בודד ("יובאו N") אי אפשר להצליב מול הקובץ.
- **פענוח סכום דורש טוקן מספרי אחד בדיוק.** מינוס נגרר (`45.00-`) וסוגריים (`(20.00)`) = זיכוי; תא עם יותר ממספר אחד (`3 תשלומים 100.00`) מוחזר כלא קריא. סיבה: הסרת כל תו שאינו ספרה הפכה זיכוי לחיוב ושרשרה ספרות לסכום מומצא (3,100).
  - נדחה: לקחת את המספר הראשון בתא — היה בוחר בשקט את מספר התשלומים.
- **`normalizeSheets` אחד לשלושת מסלולי הייבוא** (זיהוי אוטומטי / פריסט מפורש / מיפוי ידני). סיבה: שלושתם חזרו על אותו לולאת-טאבים, ורק אחד מהם דיווח פירוט per-tab.

## אחסון ואבטחה

- **ערכים בשאילתות Drive עוברים escaping**. סיבה: שם הקובץ שולב למחרוזת במרכאות בשאילתה, והוא קלט שהמשתמש שולט בו — קובץ מיובא מחופש לפי שמו.
- **מטמון מזהי קבצים ממופתח לפי תיקייה + שם**, לא שם בלבד. סיבה: העלאת קובץ בשם `db.json` הייתה מקבלת מהמטמון את מזהה בסיס הנתונים בשורש ודורסת אותו.
- **service worker נרשם** (לא היה רשום כלל — האפליקציה לא הייתה installable ולא עבדה offline).
  - **מוגבל ל-same-origin**. סיבה: fetch handler נורה גם על בקשות cross-origin, והגרסה הקודמת שמרה כל תגובת GET — כולל קריאת `db.json` מ-Drive, כלומר כל ההיסטוריה הפיננסית לדיסק בלי הצפנה, ששורדת התנתקות.
  - **נתיבי APP_SHELL יחסיים**. סיבה: GitHub Pages מגיש מתת-נתיב (`/personal-cfo/`), שם `/index.html` הוא 404 — ורשומה חסרה אחת מפילה את כל `addAll()` ואיתה את ה-install.

## התחברות

- **ניסיון התחברות שקט רק אם הדפדפן כבר נתן הסכמה בעבר**. סיבה: הניסיון פותח פופאפ Google, וכשאין סשן לנצל Google משאיר אותו פתוח על דף ההתחברות — החלון שייך לסקריפט של Google ואי אפשר לסגור אותו מהקוד. כל ביקור ראשון השאיר חלון תקוע.
  - הדגל שמור ב-localStorage ומכיל **רק** את עובדת ההסכמה — לא טוקן, לא פרטי חשבון. הטוקן נשאר בזיכרון בלבד.
  - התנתקות מבטלת הסכמה ולכן מנקה את הדגל.
  - נשקל ונדחה: ביטול ההתחברות האוטומטית לגמרי — מחיר UX למשתמשת חוזרת.
- **`error_callback` מחובר**. סיבה: פופאפ שנכשל בפתיחה או נסגר מדווח רק שם; בלעדיו שום promise לא מסתיים והמקרה מתגלה רק דרך ה-timeout.

## סיווג ותזרים

- **קטגוריה חדשה "מנויים ושירותים דיגיטליים"** (מוזיקה וסטרימינג / תוכנה ואחסון / מנויים אחרים).
  - נשקל ונדחה: תת-קטגוריה תחת "תקשורת" — המנויים היו נבלעים בשורה אחת עם חשבון הסלולר.
- **"טיפול כהוצאה קבועה" הורחב מקטגוריה בודדת לרשימה**. סיבה: בלי זה אי אפשר לסמן מנוי כקבוע בלי לקרוא לו "פיננסים וביטוח". מנוי מקבל את אותו טיפול כמו פרמיית ביטוח — מחויב בין אם השתמשת ובין אם לא.
- **PayPal לא מקבל כלל סיווג כלל.** סיבה: זה אמצעי תשלום ולא בית עסק — "כל פעם זה משהו אחר, כמו כרטיס אשראי". כלל גורף היה מסתיר את הרכישה האמיתית מאחורי שם המחייב. חיוב שכתוב עליו השירות (`PAYPAL *SPOTIFY`) נתפס לפי אותו שירות; השאר הולך לסיווג ידני.
  - **Apple ו-Google הוחרגו מאותו נימוק.** הערה: כלל קיים `GOOGLE → פנאי/תחביבים` עדיין במאגר וסובל מאותה בעיה — לא הוסר.
- **תנועה שממתינה לסיווג נשמרת ב-`parsed_transactions` מיד**, עם `needs_review: true` וקטגוריית "ממתין לסיווג ידני". סיבה: תור בזיכרון המודול הוציא את הכסף שעליה מכל סיכום בכל מסך, והיא אבדה לגמרי ברענון — בזמן שסיכום הייבוא כבר דיווח "יובאו".
  - **הקטגוריה נשארת תווית ההמתנה גם כשיש הצעה**; ההצעה נשמרת בנפרד (`suggested_category`, `suggested_sub_category`, `suggested_rule`). סיבה: לספור את הכסף זה לא אותו דבר כמו לסווג אותו — העיקרון ש"ניחוש מילת מפתח לא מוחל בשקט" נשמר.
  - **אישור סיווג מעדכן את התנועה במקומה**, לא מוסיף אותה. סיבה: היא כבר במאגר; הוספה הייתה סופרת כל שקל מסווג פעמיים.
  - `matchedRule` שונה ל-`suggested_rule` — השדה נשמר עכשיו במסד, ולכן נקרא כמו שאר השדות השמורים.
  - נדחה: להשאיר את התור בזיכרון ורק להציג את סכומו בנפרד — עדיין מאבד הכל ברענון.
- **תור הסיווג הוא תצוגה מעל המצב**, לא מחסן נפרד. נגזרת: סיווג מהמסך "כל התנועות" מוציא את התנועה גם מהתור.
- **אחוז החזר לכל תנועה** (`reimbursed_percent`), לא סימון בוליאני. סיבה: הוצאת עבודה ששולמה מהפרטי יצאה מהחשבון ומופיעה בדוח, אבל הכסף חוזר — ולפעמים רק חלקו. `100` = לא נספר בכלל, `40` = 60% נספרים.
  - **מיושם ב-`monthlyExpenseSeries` בלבד.** סיבה: כל חישובי ההוצאות עוברים דרכה, אז הגדרה אחת של "כמה זה עלה" ואין אפשרות שחישוב אחד יסטה מהשני.
  - **חריג מכוון**: טבלת הקבצים לפי חודש מציגה את הסכום **כפי שחויב**. סיבה: היא נועדה להצליב קובץ מול הדוח שממנו הגיע, וסכום מנוכה לא היה מצטלב.

## ממשק

- **ערכי הטקסונומיה עוברים escaping בבניית ה-dropdowns.** סיבה: `חופשות וחו"ל` מכיל גרש כפול שסגר את מאפיין ה-`value` באמצע; הערך נחתך ל-`חופשות וחו`, שום ערך שמור לא התאים, ובחירה נשמרה כמחרוזת ריקה.
- **הרינדור של הדשבורד פוצל**: החלפת חודש מרנדרת רק את כרטיס הקבצים. סיבה: רינדור מלא הריץ מחדש את כל חישובי התזרים ובנה מחדש את שני הגרפים כדי לעדכן טבלה אחת, וגם איבד את מיקום הגלילה.
- **טבלת הקבצים מפרידה חיובים מזיכויים** (חיובים / זיכויים / סה"כ נטו). סיבה: זיכוי הוא סכום שלילי, וקיזוזו בתוך עמודה שכותרתה "סה"כ הוצאה" הקטין בשקט את מה שבאמת יצא.
- **שורה בטבלה מציגה גם את סה"כ הקובץ כולו** ואת החלק שנופל בחודשים אחרים. סיבה: מועד חיוב שנסגר באמצע החודש פורס קובץ על שני חודשים, והחלק המוצג נראה כמו קובץ שלא הסתדר מול הדוח.
- **קובץ שנקרא חלקית מסומן ⚠ בטבלה**, עם פירוט השורות שלא נקראו. סיבה: סכום של קובץ חלקי לא יכול להיראות כמו סכום מלא.
- **תנועות ממתינות מסומנות במסך "כל התנועות"** ויש להן ערך בסינון הקטגוריות. סיבה: תווית ההמתנה אינה בטקסונומיה, ובלי זה אי אפשר לסנן דווקא למה שדורש טיפול.
- **מסך "כללי סיווג"** — צפייה ומחיקה של כללים שנלמדו. סיבה: כלל נשמר בשקט בכל סיווג ידני וגובר מאז על ברירות המחדל, בלי שום דרך לראות או להסיר אותו.
  - מוצגת **ספירת תנועות תואמות** לכל כלל, באמצעות אותה פונקציית התאמה שהייבוא משתמש בה — כדי שהמסך לא יוכל לסתור את מה שייבוא יעשה.
  - **כללים זהים מסומנים ככפולים.** סיבה: כלל נוסף בכל אישור, אז אותו בית עסק שסווג פעמיים משאיר שני כללים שרק הראשון פועל.
  - הרשימה ממוינת לפי הטקסט, לכן מחיקה ממפה חזרה למיקום השמור ולא למיקום השורה.
- **רכיב הטבלה המשותף קיבל טופס-הוספה אופציונלי**. סיבה: למסך הכללים אין מה להוסיף ידנית, והחלופה הייתה עותק שני של אותו markup.

## תשתית

- **יעד הפריסה**: GitHub Pages מ-`master`, בכתובת `https://kis-lev.github.io/personal-cfo/` — תת-נתיב, לא שורש דומיין.
- **טעינת קבצי `data/*.json` דרך מטמון משותף אחד**. סיבה: שלושה מודולים החזיקו עותק כמעט זהה של אותו fetch-וזכור. נשמרת ההבטחה עצמה ולא התוצאה, כדי ששני מסכים שמבקשים במקביל יחלקו בקשה אחת.
- **ממוצע ההוצאה לקטגוריה מחושב במעבר אחד של חלוקה לדליים**. סיבה: קודם כל קטגוריה סרקה מחדש את כל היסטוריית התנועות, כלומר קריאה חוזרת של כל התנועות כמספר הקטגוריות.
- **כל הקטגוריות מחולקות באותו מכנה** — מספר החודשים שהנתונים מכסים, לא מספר החודשים שבהם הקטגוריה הופיעה. סיבה: חיוב חד-פעמי של 3,000 ₪ בחודש אחד מתוך שישה נספר כ-3,000 ₪ בכל חודש, והשורות לא הסתכמו למספר "הכולל" שמעליהן. `monthsWithData` נשאר כעמודה, כסייג על ממוצע שנבנה ממעט חודשים.
- **מדיניות תלויות נשמרה**: אפס תלויות npm. הקוד החיצוני היחיד בזמן ריצה הוא ספריית ה-OAuth הרשמית של Google.

## תקופות חודשיות

- **חודש של הוצאות = מחזור חיוב, 10 עד 10**, על שם החודש שבו הוא מתחיל. מחזור אוגוסט = 10/08 עד 09/09. סיבה: מה שקובע מתי הכסף יצא מהחשבון הוא מחזור החיוב של האשראי, לא הלוח. נגזרת: חיוב ב-3 בספטמבר שייך למחזור אוגוסט.
  - **היום ה-10 פותח את המחזור החדש** (אושר במפורש) — 10/08 הוא היום הראשון של מחזור אוגוסט.
- **חודש של הכנסות = חודש לוח.** הכנסה בין 1 ל-31 באוגוסט שייכת לאוגוסט. סיבה: הזזת ה-10→10 היא תכונה של חיוב אשראי; משכורת לא עוברת דרכו.
  - היום אין תנועות הכנסה מיובאות (ההכנסות הקבועות הן סכום חודשי בלי תאריך), ולכן זהו תפר לייבוא עו"ש עתידי ולא ניחוש על שורות קיימות. תנועה נחשבת הכנסה רק אם היא מסומנת כך במפורש; **סכום שלילי אינו הכנסה** אלא זיכוי, והוא שייך למחזור שבו זוכה.
- **`engine/periods.js` הוא הבעלים היחיד של השאלה "לאיזה חודש התנועה שייכת".** סיבה: שתי הגדרות שונות של "חודש" באותה מערכת מתבלבלות זו בזו ברגע שמישהו כותב `date.slice(0, 7)` במקום נוסף.
- **המחזור מוצג תמיד עם הטווח** ("08/2026 (10/08–09/09)"). סיבה: "08/2026" לבד נקרא כאוגוסט לוחי, וכל העניין הוא שזה לא.

## קריאת קבצים — המשך

- **כל חיפוש תג ב-`xlsx-parser` מתעלם מקידומת namespace.** סיבה: דוח שנכתב כ-`<x:row>` במקום `<row>` נקרא כגיליון עם אפס שורות, כי `getElementsByTagName` על מסמך XML מתאים לשם המלא. הקובץ כולו יובא כריק ודווח כ"לא זוהה פורמט". אחרי התיקון, ה-`.xlsx` וה-`.xlsm` של אותו דוח נקראים זהה.
- **סריאל אקסל מתקבל כתאריך רק בטווח סביר.** סיבה: שורה אחרונה כמו `["60", "", "", "7393.0"]` הפכה את מונה השורות ל-28/02/1900.
- **שורת הסיכום של הקובץ מזוהה חשבונאית**: שורה בלי תאריך שסכומה שווה לסכום כל מה שנקרא מאותו טאב. סיבה: היא נשאה סכום קריא ותו לא, ולכן דווחה כ"כסף שלא נקרא" בכל ייבוא — אזעקת שווא שמאמנת להתעלם מהסימון. נגזרת חיובית: הסיכום של המנפיק הופך לבדיקת הצלבה אוטומטית מול מה שנקרא.
  - **סיכום שלא מסתדר נשאר אי-התאמה רועשת** ולא מסווג מחדש בשקט — זה בדיוק המקרה שבו באמת חסרות שורות.
  - נדחה: זיהוי לפי הנוסח ("סך הכל") — תלוי בשפה ובניסוח של כל מנפיק.
- **שורה שנפסלה נושאת `kind`**: `unread` (מזוהה בתאריך או בבית עסק — אולי תנועה שאבדה), `not_a_transaction` (כותרת/תנאים משפטיים), `statement_total`. סיבה: סימון אדום על רהיטי הדוח בכל ייבוא מאמן להתעלם מהאזהרה שכן חשובה. כולן עדיין נספרות ומוצגות.

---

## פתוח / לבדיקה

- תנועות Spotify שכבר יובאו נושאות את הקטגוריה הישנה — לא מהגרות לבד.
- כלל שנלמד ונשמר ב-Drive גובר על ברירות המחדל; אם קיים כלל ישן ל-Spotify, השינוי לא ייכנס לתוקף עד שיימחק.
- ייתכנו תנועות ב-`חופשות וחו"ל` עם תת-קטגוריה ריקה, שנמחקה על ידי באג ה-escaping לפני שתוקן.
- הכלל `GOOGLE → פנאי/תחביבים` עדיין קיים ומתנהג כמו כלל גורף על אמצעי תשלום.
- `persistState()` נקרא בלי catch בכמה מקומות; בלי הקשר Drive זה יוצר unhandled rejection.
- תנועות שהמתינו לסיווג לפני המעבר לשמירה במאגר מעולם לא נשמרו — הן אינן ב-`db.json` ולא ישוחזרו לבד. העלאה חוזרת של אותם קבצים היא הדרך היחידה; הדה-דופליקציה תחסום רק את מה שכבר קיים.
- `db.json` שנוצר לפני `import_log` מנורמל ל-`[]`, כלומר לקבצים שיובאו בעבר אין דיווח על שורות שלא נקראו — גם אם היו כאלה.
- ההחלטה שתא סכום דו-משמעי מוחזר כלא קריא מרחיבה את מה שנספר כ"שורה שלא נקראה"; פורמט שבו זו תבנית רגילה יידרש לפריסט/עמודה אחרת.
- **עסקאות בתשלומים נופלות כולן במחזור של תאריך הרכישה.** נצפה בדוחות 7484: אותה רכישה מופיעה בשני דוחות עוקבים כ"תשלום 3 מתוך 5" ו"תשלום 4 מתוך 5", עם מספרי שובר שונים (ולכן נכון שאינן נחשבות כפילות) אבל עם אותו `תאריך רכישה` — כך ששני התשלומים נספרים באותו מחזור, למרות שהכסף יצא בשני מחזורים שונים. הקובץ אינו נושא עמודת מועד חיוב; מועד החיוב מופיע רק בשם הקובץ. דורש החלטה.

---

מי שממשיך מכאן: לאמת מול הקוד, לא מול המסמך.
